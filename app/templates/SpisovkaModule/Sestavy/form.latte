{var Select2 => true}
{block title}{$nadpis}{/block}
{block content}

    <div class="navigate">
    <a href="{link :Spisovka:Sestavy:default}">Sestavy</a> - {$nadpis}
    </div>

    <h2>{$nadpis}</h2>

    {$form->render('begin')}
    <div class="dokument_blok_hlavni">
        <div class="h2">
            Sestava
        </div>
        {input2 sestava_nazev}
        {input2 sestava_popis}
        {input2 sestava_typ}
        <dl class="detail_item">
            <dt>{$form['sestava_filtr']->caption}</dt>
            <dd>{$form['sestava_filtr']->controlPart} (zobrazit výběr rozsahu podle pořadového čísla nebo data)</dd>
        </dl>
    </div>

    <div class="dokument_blok_sedy">
        <div class="h2">
            Zobrazované údaje
        </div>
        {input2 zobrazeni_cas}
        {input2 zobrazeni_adresa}
        <p><strong>Výběr sloupců:</strong></p>
        {input2 sloupce_smer_dokumentu}
        {input2 sloupce_poznamka}
        {input2 sloupce_poznamka_predani}
        {input2 sloupce_prazdny}
    </div>
    
    {include ../Vyhledat/form_common.latte}

    {include ../Vyhledat/form_predani.latte}
 
    {include ../Vyhledat/form_subjekt.latte}

    <div class="dokument_blok_submit">
        {$form['odeslat']->control}
        &nbsp;&nbsp;
        {$form['storno']->control}
    </div>
    {$form->render('end')}

    <div id="dialog"></div>