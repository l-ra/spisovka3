@{block title}Zprávy{/block}
@{block content}
    <h2>Zprávy</h2>
    
    {if count($seznam)>0}
        {foreach $seznam as $zprava}
        <div class="flash_infozprava">
        </div>
        {/foreach}
    {widget vp}
    {else}
    <div class="prazdno">Nebyly zjištěny žádné zprávy.</div>
    {/if}    