{block title}Dokument{/block}
{block content}
    <div id="error">
        <h2>Nejste oprávněn zobrazit tento {if strpos(':Spisy', $presenter->name)}spis{else}dokument{/if}</h2>
        <p>
        </p>
    </div>